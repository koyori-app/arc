# Benchmark artifacts (generated)

Run `bash scripts/run-3layer-bench.sh` to regenerate.

- `results/` — criterion log, per-layer JSON, merged report
- `fixtures/` — exported JSON (via `cargo run --example export_fixtures`)
